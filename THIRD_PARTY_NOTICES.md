# Third-party notices

The distributed plugin bundle has no bundled runtime npm dependencies.

It interacts with:

- the Obsidian plugin API, supplied by the host application;
- the Yandex Disk REST API, supplied as an external service;
- Node.js networking modules on desktop only, supplied by the desktop runtime.

Service names and trademarks remain the property of their respective owners. Their mention describes interoperability and does not imply affiliation or endorsement.
