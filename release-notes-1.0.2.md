# AIMETON Cloud Bridge 1.0.2

Mobile compatibility rollback release.

- Rebased runtime on the previously verified mobile core.
- Removed SecretStorage-dependent startup changes.
- Added safe boot when `data.json` is incompatible or corrupted.
- Kept the AIMETON identity, bilingual UI, previews, transfer progress, and desktop proxy support.
